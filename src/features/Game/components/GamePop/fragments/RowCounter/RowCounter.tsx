'use client';

import type { FC } from 'react';
import { RowCounterStyled } from './Styled';

type PropsType = {
  
};

const RowCounter: FC<PropsType> = ({ }) => {
  return (
    <RowCounterStyled>
	
    </RowCounterStyled>
  );
};

export default RowCounter;
